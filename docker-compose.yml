version: "3.8"

services:
  web:
    build: .
    ports:
      - "5050:5050"
    environment:
      - DATABASE_URL=mysql+pymysql://user:password@db/deinedb
      - SECRET_KEY=dein-geheimer-schlüssel
    depends_on:
      - db

  db:
    image: mariadb:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: deinedb
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  db_data:
